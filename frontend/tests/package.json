{
  "name": "summit-frontend-tests",
  "version": "2.0.0",
  "private": true,
  "description": "Zero-dependency frontend regression guards. Each file is a plain Node script with its own t() harness — no jest, no install.",
  "scripts": {
    "test": "node smoke.test.js && node filter.test.js && node mobile.test.js && node joblist.test.js"
  }
}
